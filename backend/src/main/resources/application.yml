logging:
  level:
    org:
      hibernate:
        SQL: debug
spring:
  devtools:
    restart:
      trigger-file: reload
    livereload:
      enabled: false
  jackson:
    default-property-inclusion: NON_NULL
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      format-sql: true
      use-sql-comments: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        hibernate.spatial.dialect: h2

project:
  jwt:
    signKey: Bu:GW8bgPlEw
    expirationMinutes: 1440