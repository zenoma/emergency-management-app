get:
  summary: Lista de logs de los equipos
  operationId: findTeamLogsByQuadrantIdAndDate
  tags:
    - LogManagement
  security:
    - bearerAuth: [ ]
  parameters:
    - in: query
      name: quadrantId
      schema:
        type: integer
      required: true
    - in: query
      name: startDate
      schema:
        type: string
        format: localDate
      required: true
    - in: query
      name: endDate
      schema:
        type: string
        format: localDate
      required: true
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../logManagement/schemas/responses/TeamQuadrantLogDto.yml"
