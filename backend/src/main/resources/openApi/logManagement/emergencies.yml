get:
  summary: Lista de logs de emergencias
  operationId: findAllEmergenciesLog
  tags:
    - LogManagement
  security:
    - bearerAuth: [ ]
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: "../logManagement/schemas/EmergencyQuadrantLogSchema.yml"


