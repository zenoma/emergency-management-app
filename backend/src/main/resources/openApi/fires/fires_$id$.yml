get:
  summary: Información de un incendio por ID
  operationId: getFireById
  tags:
    - FireManagement
  security:
    - bearerAuth: [ ]
  parameters:
    - in: path
      name: id
      required: true
      schema:
        type: integer
        format: int64
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: './schemas/responses/FireResponseDto.yml'