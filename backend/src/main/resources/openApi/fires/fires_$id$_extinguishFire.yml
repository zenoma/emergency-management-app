post:
  summary: Extinguir un incendio extinguiendo los cuadrantes afectados
  operationId: postExtinguishFire
  tags:
    - FireManagement
  security:
    - bearerAuth: [ ]
  parameters:
    - in: path
      name: id
      required: true
      schema:
        type: integer
        format: int64
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: './schemas/responses/FireResponseDto.yml'