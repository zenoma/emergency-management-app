get:
  summary: Información de una organización por ID
  operationId: getOrganizationById
  tags:
    - OrganizationManagement
  security:
    - bearerAuth: [ ]
  parameters:
    - in: path
      name: id
      required: true
      schema:
        type: integer
        format: int64
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: './schemas/responses/OrganizationTypeResponseDto.yml'
