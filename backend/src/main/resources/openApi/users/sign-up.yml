post:
  summary: Registro (signUp)
  operationId: postUsersSignUp
  tags:
    - UserManagement
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: './schemas/requests/RequestUserDto.yml'
  responses:
    '201':
      description: Created
      content:
        application/json:
          schema:
            $ref: './schemas/responses/AuthenticatedUserDto.yml'
