get:
  summary: Lista usuarios
  operationId: getUsers
  tags:
    - UserManagement
  security:
    - bearerAuth: [ ]
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: './schemas/responses/UserDto.yml'
