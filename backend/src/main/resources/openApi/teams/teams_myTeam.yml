get:
  summary: Obtener el equipo al que pertenece el usuario autenticado
  operationId: getTeamByUserId
  tags:
    - TeamManagement
  security:
    - bearerAuth: [ ]
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: './schemas/responses/TeamResponseDto.yml'
