get:
  summary: Lista de equipos activos
  operationId: getActiveTeamsByOrganizationId
  tags:
    - TeamManagement
  parameters:
    - in: query
      name: organizationId
      schema:
        type: integer
        format: int64
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: './schemas/responses/TeamResponseDto.yml'
