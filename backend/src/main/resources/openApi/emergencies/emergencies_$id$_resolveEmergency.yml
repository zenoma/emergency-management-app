post:
  summary: Resolver una emergencia desvinculando los cuadrantes afectados
  operationId: postResolveEmergency
  tags:
    - EmergencyManagement
  security:
    - bearerAuth: [ ]
  parameters:
    - in: path
      name: id
      required: true
      schema:
        type: integer
        format: int64
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: './schemas/responses/EmergencyResponseDto.yml'
