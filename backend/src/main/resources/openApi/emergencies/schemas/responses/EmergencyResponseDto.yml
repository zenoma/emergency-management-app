type: object
required:
  - id
  - description
  - emergencyTypeName
  - emergencyIndex
  - createdAt
  - resolvedAt
properties:
  id:
    type: integer
    format: int64
  description:
    type: string
  emergencyTypeName:
    type: string
  emergencyIndex:
    type: string
    enum: [ RESUELTO, CERO, UNO, DOS, TRES ]
  createdAt:
    type: string
    format: localDateTime
  resolvedAt:
    type: string
    format: localDateTime
  location:
    $ref: "../../../common/schemas/CoordinatesDto.yml"
  quadrantInfo:
    type: array
    items:
      $ref: "../../../quadrants/schemas/responses/QuadrantDto.yml"
