get:
  summary: Get all available emergency types
  operationId: getAllEmergencyTypes
  tags:
    - EmergencyManagement
  security:
    - bearerAuth: [ ]
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: './schemas/responses/EmergencyTypeDto.yml'
