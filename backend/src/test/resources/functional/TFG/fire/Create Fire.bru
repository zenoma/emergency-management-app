meta {
  name: Create Fire
  type: http
  seq: 3
}

post {
  url: {{base_url}}/fires
  body: json
  auth: inherit
}

body:json {
  {
      "description": "aaa",
      "type": "aaaa"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
