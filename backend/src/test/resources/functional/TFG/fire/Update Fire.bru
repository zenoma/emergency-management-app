meta {
  name: Update Fire
  type: http
  seq: 6
}

put {
  url: {{base_url}}/fires/16
  body: json
  auth: inherit
}

body:json {
  {
      "description": "eee",
      "type": "eee",
      "fireIndex": "EXTINGUIDO"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
