meta {
  name: Send FMC token
  type: http
  seq: 8
}

post {
  url: {{base_url}}/users/1/mobileDevice
  body: json
  auth: inherit
}

body:json {
  {
    "fcmToken": "string"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
