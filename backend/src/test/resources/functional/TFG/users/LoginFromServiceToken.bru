meta {
  name: LoginFromServiceToken
  type: http
  seq: 3
}

post {
  url: {{base_url}}/users/loginFromServiceToken
  body: multipartForm
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
