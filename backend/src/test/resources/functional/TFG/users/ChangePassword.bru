meta {
  name: ChangePassword
  type: http
  seq: 4
}

post {
  url: {{base_url}}/users/1/changePassword
  body: json
  auth: inherit
}

body:json {
  {
      "oldPassword":"4",
      "newPassword":"4"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
