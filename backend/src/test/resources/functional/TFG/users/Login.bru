meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{base_url}}/users/login
  body: json
  auth: none
}

body:json {
  {
      "userName": "4@email.com",
      "password": "4"
  }
}

script:post-response {
  bru.setEnvVar("serviceToken", res.body.serviceToken);
}

settings {
  encodeUrl: true
  timeout: 0
}
