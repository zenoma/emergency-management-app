meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{base_url}}/users/login
  body: json
  auth: none
}

body:json {
  {
      "userName": "coord.galicia@example.com",
      "password": "abc1234"
  }
}

script:post-response {
  bru.setEnvVar("token", res.body.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
