meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{base_url}}/users/login
  body: json
  auth: none
}

body:json {
  {
      "userName": "4@email.com",
      "password": "abc1234"
  }
}

script:post-response {
  bru.setEnvVar("token", res.body.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
