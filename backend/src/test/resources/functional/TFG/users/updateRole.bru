meta {
  name: updateRole
  type: http
  seq: 5
}

post {
  url: {{base_url}}/users/:targetUserId/updateRole
  body: json
  auth: inherit
}

params:path {
  targetUserId: 2
}

body:json {
  {
      "userRole": "MANAGER"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
