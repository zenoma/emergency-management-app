meta {
  name: updateRole
  type: http
  seq: 5
}

post {
  url: {{base_url}}/users/2/updateRole
  body: json
  auth: inherit
}

body:json {
  {
      "userRole": "MANAGER"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
