meta {
  name: Create organizationType
  type: http
  seq: 3
}

post {
  url: {{base_url}}/organizationTypes
  body: json
  auth: inherit
}

body:json {
  {
      "name": "Brigada"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
