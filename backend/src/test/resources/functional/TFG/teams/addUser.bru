meta {
  name: addUser
  type: http
  seq: 9
}

post {
  url: {{base_url}}/teams/3/addUser
  body: json
  auth: inherit
}

body:json {
  {
      "memberId": "4"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
