meta {
  name: addUser
  type: http
  seq: 9
}

post {
  url: {{base_url}}/teams/1/addUser/
  body: json
  auth: inherit
}

body:json {
  {
      "memberId": "2"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
