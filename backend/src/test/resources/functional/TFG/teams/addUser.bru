meta {
  name: addUser
  type: http
  seq: 10
}

post {
  url: {{base_url}}/teams/5/addUser
  body: json
  auth: inherit
}

body:json {
  {
      "memberId": "4"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
