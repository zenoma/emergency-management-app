meta {
  name: Create team
  type: http
  seq: 2
}

post {
  url: {{base_url}}/teams
  body: json
  auth: inherit
}

body:json {
  {
    "code": "nuevo",
    "organizationId": 1
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
