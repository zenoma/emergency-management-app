meta {
  name: deleteUser
  type: http
  seq: 11
}

post {
  url: {{base_url}}/teams/3/deleteUser
  body: json
  auth: inherit
}

body:json {
  {
      "memberId": "4"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
