meta {
  name: Update status notice
  type: http
  seq: 8
}

put {
  url: {{base_url}}/notices/30/status
  body: json
  auth: inherit
}

body:json {
  {
      "status": "ACCEPTED"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
