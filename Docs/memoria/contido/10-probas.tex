\chapter{Pruebas}
\label{chap:probas}

\lettrine{L}{as} pruebas de software constituyen un pilar fundamental para garantizar la calidad, la corrección y la estabilidad del sistema desarrollado. En este proyecto se ha adoptado un enfoque de pruebas automatizadas para los tres subsistemas, combinando distintos niveles de prueba según la naturaleza de cada uno. Este capítulo describe la metodología empleada, las tecnologías utilizadas y el detalle de las pruebas realizadas en cada subsistema.

\section{Backend}

Las pruebas del backend se dividen en dos categorías: pruebas unitarias, que verifican la lógica de negocio de forma aislada mediante simulación de dependencias, y pruebas de integración, que comprueban el funcionamiento conjunto de los servicios con una base de datos real. Ambas están totalmente automatizadas y se ejecutan como parte del ciclo de compilación con Maven, lo que permite detectar problemas de regresión de forma temprana.

Tanto las pruebas unitarias como las de integración siguen una serie de convenciones. La nomenclatura de los métodos de prueba respeta el patrón \acrfull{tdd} con las claves \textit{given} (estado inicial), \textit{when} (acción que se prueba) y \textit{then} (resultado esperado). Además, se emplea el patrón \textit{Arrange-Act-Assert} para estructurar cada prueba de forma clara y legible, facilitando la comprensión de su propósito y lógica. 

\subsection{Pruebas unitarias}

Las pruebas unitarias se han implementado con JUnit 5 y Mockito. Este último permite simular los repositorios y dependencias externas de cada servicio, de modo que las pruebas se centran exclusivamente en la lógica de negocio sin involucrar la persistencia ni la base de datos. Esto permite validar casos límite, reglas de negocio y flujos de error de forma rápida y aislada, asegurando que cada unidad de código se comporta correctamente bajo diferentes escenarios. 

Para la creación de entidades de prueba se emplea el patrón \textit{Object Mother}, que proporciona fábricas estáticas con valores por defecto y métodos de personalización, facilitando la creación estándar y a la vez flexible de objetos sin saturar cada prueba con lógica de inicialización.

La siguiente figura \ref{fig:object-mother} muestra un ejemplo de clase que sigue el patrón Object Mother para la entidad \texttt{Organization}, con métodos estáticos para generar instancias preconfiguradas y personalizables. Este patrón tiene numerosas ventajas como la reutilización de código, mantenibilidad y alta legibilidad, y promueve la separación entre la creación de datos de prueba y las pruebas en sí. 

En concreto se puede observar que la clase \texttt{OrganizationMother} proporciona un método \texttt{withDefaultValues()} que devuelve una instancia de \texttt{Organization} con valores por defecto, y un método \texttt{withNames(List names)} que permite personalizar el nombre de la organización. Esto permite a las pruebas crear rápidamente instancias válidas de \texttt{Organization} sin repetir código de inicialización en cada test. También existe un método \texttt{withOrganizationTypeAndRandomNames(String name)} que permite crear una organización con un tipo específico y nombres aleatorios, lo que es útil para pruebas que requieren variabilidad en los datos.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imaxes/organizacion-om.png}
  \caption{Código de una clase que sigue el patrón Object Mother}
  \label{fig:object-mother}
\end{figure}


\subsection{Pruebas de integración}

Las pruebas de integración utilizan Testcontainers con la imagen \textit{postgis/postgis}, que levanta una instancia efímera de PostgreSQL con PostGIS para cada ejecución. La base de datos se inicializa automáticamente con los mismos scripts SQL de creación de tablas, cuadrantes y datos de inicialización que se emplean en desarrollo (\texttt{01\_create\_tables.sql}, \texttt{02\_init\_quadrants.sql} y \texttt{03\_init\_data.sql}), lo que garantiza que el entorno de pruebas es representativo del real. Todas las pruebas de integración extienden la clase base abstracta \texttt{IntegrationTest}, que configura \texttt{@SpringBootTest}, \texttt{@Transactional} y la inyección automática de propiedades de conexión al contenedor PostgreSQL.

Esta configuración de las pruebas de integración permite validar la lógica de negocio en un entorno realista, con una base de datos real y sin simulaciones. Se han diseñado pruebas para cubrir tanto los flujos normales como los casos de error, incluyendo validaciones de negocio, manejo de excepciones y casos límite. 


\subsection{Cobertura de las pruebas}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imaxes/coverage.png}
  \caption{Cobertura de las pruebas del backend}
  \label{fig:coverage}
\end{figure}

En esta imagen \ref{fig:coverage} se muestra la cobertura de las pruebas unitarias e integración del backend. La cobertura es un indicador que aporta confianza pero no garantiza la ausencia de errores, por lo que se ha complementado con un análisis de casos límite y validaciones de negocio en las pruebas de integración. Se ha tratado de alcanzar una cobertura elevada en la capa de servicios mediante una batería de tests de integración que validan tanto los flujos normales como los casos de error, asegurando que la lógica de negocio se comporta correctamente en un entorno realista con una base de datos real. 

Los servicios que no alcanzaron una cobertura del 100\% fueron aquellos que cuentan con una gran cantidad de validaciones de negocio y reglas complejas, como \texttt{AssignmentService} y \texttt{EmergencyService}. En estos casos, se priorizó la cobertura de los flujos críticos y de los casos de error más relevantes, dejando algunas ramas menos probables sin cubrir.


\section{Frontend}

Para el frontend se ha adoptado una estrategia de pruebas automatizadas centrada en dos niveles: la lógica pura de la aplicación (funciones de validación, transformación de coordenadas y reductores de estado) y el renderizado de componentes clave. 

Las pruebas se han implementado con Vitest, el ejecutor de tests nativo de Vite. Para el renderizado de componentes se ha empleado React Testing Library, que fomenta pruebas centradas en el comportamiento observable desde la perspectiva del usuario en lugar de detalles de implementación interna.

Se han creado tests distribuidos en las siguientes categorías:

\begin{itemize}
  \item \textbf{Lógica pura:} funciones de validación de formularios (email, teléfono, DNI), formateo de fechas y transformación de coordenadas geográficas a UTM (y viceversa). Estos tests verifican la corrección de la lógica de negocio en el frontend sin necesidad de montar componentes ni depender del navegador, lo que los hace extremadamente rápidos y fiables.
  \item \textbf{Reductores de estado:} pruebas sobre los componentes de Redux (\texttt{LoginSlice}, \texttt{themeSlice}) que validan el estado inicial, las transiciones de estado y los selectores.
  \item \textbf{Componentes de interfaz:} renderizado de componentes clave como \texttt{EmergencyTypeIcon} (mapeo de nombres de emergencia a iconos), \texttt{BackButton}, \texttt{NotFoundPage} y \texttt{TeamNotFoundPage}. Estos tests verifican que los componentes se renderizan sin errores, que muestran los elementos esperados (iconos, enlaces, imágenes) y que responden correctamente a las propiedades recibidas.
\end{itemize}

La siguiente figura \ref{fig:vitest} muestra la ejecución completa de los tests del frontend con Vitest:

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imaxes/vitest.png}
  \caption{Ejecución de los tests del frontend con Vitest}
  \label{fig:vitest}
\end{figure}

Esta batería de tests automatizados aporta tres beneficios principales. En primer lugar, permite detectar de forma temprana regresiones en la lógica de validación y formateo, que son propensas a errores silenciosos. En segundo lugar, garantiza que los componentes críticos se renderizan correctamente tras cambios en las dependencias o en la estructura del código. Por último, al ejecutarse con Vitest, se integran en el flujo de desarrollo con una ejecución rápida que no interrumpe el trabajo del desarrollador.

Además de las pruebas automatizadas, se ha complementado la validación con un plan de pruebas manuales exhaustivas de la interfaz, que permiten validar la experiencia de usuario, la usabilidad y el comportamiento visual en diferentes dispositivos y navegadores. Dado que el frontend presenta una interfaz gráfica compleja con mapas interactivos y componentes dinámicos, las pruebas manuales cubren aspectos que las pruebas automatizadas no alcanzan, como la correcta visualización del mapa, la respuesta a eventos de usuario y la navegación entre pantallas.

\section{Aplicación Android}

Para la aplicación Android se ha adoptado una estrategia de pruebas automatizadas que aprovecha las librerías de testing ya presentes en el proyecto sin añadir dependencias adicionales. Siguiendo la clasificación estándar de Android, que distingue entre pruebas locales (ejecutadas en la JVM del equipo de desarrollo) y pruebas instrumentadas (ejecutadas en un dispositivo o emulador), se han implementado dos niveles de prueba que cubren tanto la lógica pura de utilidades como la interacción con componentes de la plataforma.

\subsection{Pruebas unitarias locales}

Las pruebas unitarias locales verifican componentes que no dependen del \textit{framework} de Android o cuyas dependencias son puramente de lógica. Se ejecutan directamente sobre la JVM del desarrollador, lo que las hace extremadamente rápidas y no requieren ni emulador ni dispositivo físico. 

Todas estas pruebas se ejecutan con JUnit 4, que es la versión incluida en las dependencias del proyecto. Al ser pruebas que se ejecutan en la JVM local, no requieren configuraciones adicionales ni emulador, y pueden lanzarse desde Android Studio o mediante Gradle con la tarea \texttt{testDebugUnitTest}.


\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imaxes/coverage-android.png}
  \caption{Cobertura de las pruebas unitarias locales}
  \label{fig:android-coverage}
\end{figure}

Como se puede ver en los porcentajes de cobertura de la imagen \ref{fig:android-coverage}, estas pruebas unitarias locales alcanzan valores elevados. Estas utilidades y sus pruebas son fundamentales para garantizar la corrección de operaciones críticas como el tratamiento de coordenadas geográficas, la gestión de fechas y horas, y la persistencia de datos en SharedPreferences. 

\subsection{Pruebas instrumentadas de integración}

Las pruebas instrumentadas verifican el comportamiento de las actividades de Android en un entorno real (emulador o dispositivo físico). A diferencia de las pruebas unitarias locales, que se ejecutan directamente sobre la JVM del desarrollador y no tienen acceso al \textit{framework} de Android, las pruebas instrumentadas se despliegan en un dispositivo o emulador donde se instala el APK de la aplicación junto con un APK de test. Esto permite que las pruebas interactúen con el ciclo de vida de las actividades, las preferencias compartidas y los \texttt{Intent} extras, validando el comportamiento real de la aplicación en un contexto de ejecución real. Los \texttt{Intent} son fundamentales en Android para la comunicación entre actividades y servicios. Son el mecanismo principal para lanzar nuevas pantallas y pasar datos entre ellas. Estas pruebas aseguran que los \texttt{Intent} se reciben y manejan correctamente, incluyendo la validación de los datos que transportan.

Estas pruebas utilizan \texttt{ActivityScenario} de AndroidX Test para lanzar y controlar el ciclo de vida de las actividades, y se ejecutan con el runner \texttt{AndroidJUnit4} que proporciona \texttt{androidx.test.ext:junit}. Desde Android Studio pueden lanzarse seleccionando cualquiera de las clases en el directorio \texttt{src/androidTest/} y ejecutándolas con \texttt{Run}, o desde línea de comandos con la tarea de Gradle \texttt{connectedDebugAndroidTest}.

Se han diseñado pruebas instrumentadas para cada una de las actividades del proyecto, cubriendo la validación de la correcta recepción y manejo de los \texttt{Intent} que las lanzan, la persistencia de datos en \texttt{SharedPreferences} y la respuesta a eventos del ciclo de vida. 

Estas pruebas son fundamentales para garantizar que la aplicación se comporta correctamente en dispositivos reales, que las actividades responden a los \texttt{Intent} esperados y que la persistencia de datos funciona como se espera. Al ejecutarse en un emulador o dispositivo físico con el \textit{framework} de Android completo, estas pruebas detectan problemas que las pruebas unitarias locales no pueden identificar, como el correcto parseo de JSON, el manejo de extras opcionales en los \texttt{Intent} y la gestión del ciclo de vida ante cambios de configuración.


\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imaxes/coverage-android-instrumento.png}
  \caption{Resultado de las pruebas de la aplicación Android}
  \label{fig:android-coverage-instrumento}
\end{figure}

La imagen \ref{fig:android-coverage-instrumento} muestra el reporte que se obtiene tras ejecutar las pruebas instrumentadas. Estas pruebas aportan una capa adicional de confianza al validar el comportamiento real de la aplicación en un entorno auténtico.


\newpage
\section{Conclusión de las pruebas}

En conclusión, la estrategia de pruebas adoptada en este proyecto combina pruebas unitarias, de integración y de interfaz para garantizar la calidad y estabilidad del sistema en sus tres subsistemas: backend, frontend y aplicación Android. La automatización de las pruebas permite detectar regresiones de forma temprana, mientras que las pruebas manuales complementan la validación de la experiencia de usuario y la usabilidad. La cobertura de pruebas alcanzada, junto con la validación de casos límite y flujos críticos, proporciona un alto nivel de confianza en la corrección y robustez del sistema desarrollado.

No se ha pretendido un sistema con cobertura del 100\%, sino un conjunto de pruebas que cubran los flujos críticos y los casos de error más relevantes, priorizando la calidad y la mantenibilidad del código. La combinación de pruebas automatizadas y manuales asegura que el sistema cumple con los requisitos funcionales y no funcionales, ofreciendo una experiencia de usuario satisfactoria y un comportamiento estable en producción.

